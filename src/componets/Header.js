const Header = () => {
    //A component just for the title to remain visible at all times.
    return (
        <header className='header'>
            <h1>DM Helper</h1>
        </header>
    )
}

export default Header